{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the samloader desktop UI",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
